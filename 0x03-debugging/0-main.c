#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	int c;

	i = 0;
	c = positive_or_negative(int c); 
	printf("%c", c);
	return (0);
}

#include <stdio.h>
/**i
 * _putchar - display _putchar
 *
 * Return: Always 0 (Success)
 */
int _putchar(void)
{
	int word;

	word = 'p';
	putchar(word);
	word = 'u';
	putchar(word);
	word = 't';
	putchar(word);
	word = 'c';
	putchar(word);
	word = 'h';
	putchar(word);
	word = 'a';
	putchar(word);
	word = 'r';
	putchar(word);
	putchar('\n');
}

#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int word;

	word = '_';
	putchar(word);
	word = 'p';
	putchar(word);
	word = 'u';
	putchar(word);
	word = 't';
	putchar(word);
	word = 'c';
	putchar(word);
	word = 'h';
	putchar(word);
	word = 'a';
	putchar(word);
	word = 'r';
	putchar(word);
	putchar('\n');
	return (0);
}

#include <stdio.h>
/**
 * abs - display absolute number
 * @int: The integer to be computed
 * Return: The absolute value of the integer
 */
int _abs(int)
{
	if (int < 0 || int < 0)
		return (int);
	else
		return (0);
}

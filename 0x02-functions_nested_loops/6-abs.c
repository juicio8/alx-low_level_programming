#include <stdio.h>
#include <stdlib.h>
/**
 * abs - display absolute number
 * @n: The integer to be computed
 * Return: The absolute value of the integer
 */
int _abs(int n)
{
	int a;

	if (n < 0 || n < 0)
		a = abs (n);
		return (a);
	else
		return (0);
}

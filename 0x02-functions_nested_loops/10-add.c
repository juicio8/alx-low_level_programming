#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers and display result
 * @a: int type number
 * @b: int type number
 * Return: return sum of both integers
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
